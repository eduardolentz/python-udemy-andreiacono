# TUPLES --> Tuplas
    # Mantem uma sequencia
    # Itens não podem ser alterados --> INMUTABLE
    # tuples é mais rapida e gasta menos memorias que as listas
    
lista = [10,20,30,40]
tupla = (50,60,70,80)

# tupla.append(100) --> error
lista.append(100)

# tupla.remove(50) --> error
lista.remove(10)

print(lista)